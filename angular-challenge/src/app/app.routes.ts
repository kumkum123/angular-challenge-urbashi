import { Routes } from '@angular/router';
import { AircraftDataComponent } from './core/aircraft-data/aircraft-data.component';

export const routes: Routes = [{ path: '', component: AircraftDataComponent }];
